services:
  web-scraper:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    shm_size: '2gb'  # Shared memory for Chrome
    mem_limit: 2g